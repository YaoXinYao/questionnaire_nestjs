import { HttpInterceptorInterceptor } from './http-intercetor.interceptor';

describe('HttpIntercetorInterceptor', () => {
  it('should be defined', () => {
    expect(new HttpInterceptorInterceptor()).toBeDefined();
  });
});
